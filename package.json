{
  "scripts": {
    "prepare": "husky install"
  },
  "lint-staged":{
    "**/*.{js,jsx,ts,tsx}":[
      "npx prettier --write",
      "npx eslint --fix"
    ]
  },
  "devDependencies": {
    "eslint-config-prettier": "9.1.0",
    "husky": "^8.0.0",
    "lint-staged": "^15.2.2",
    "prettier": "3.2.5"
  }
}
