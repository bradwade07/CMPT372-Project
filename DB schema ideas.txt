products(productId SERIAL, name STRING, imgSrc STRING, description STRING, addedOn DATE, userEmail STRING)
productPrice(productId INTEGER, basePrice FLOAT, currentPrice FLOAT)

productTags(productId INTEGER, tagId INTEGER, PRIMARY KEY (productId, tagId), FOREIGN KEY productId REFERENCES products, FORIEGN KEY tagId REFERENCES tags)
tags (tagId SERIAL, tagName STRING)

userInfo(userEmail STRING PRIMARY KEY, address STRING)
userCart(userEmail STRING PRIMARY KEY, productId INTEGER, quantity INTEGER)

userType(userEmail STRING PRIMARY KEY, typeId INTEGER)
userTypes(typeId SERIAL, type STRING)

userWishlist(userEmail STRING PRIMARY KEY, productId INTEGER)

warehouses(warehouseId SERIAL, latitude FLOAT, longitude FLOAT)
warehouseStock(warehouseId INTEGER, productId SERIAL, stock INTEGER)